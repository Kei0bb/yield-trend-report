# 異常検知の閾値設定。編集後はサーバ再起動で反映されます。
# 警告は fail bin の急増のみ。yield 低下は Dashboard の ▼ 列で見るため警告にしません。
defaults:
  bin_surge:
    delta_pct: 5.0          # (最新Bin% - 過去平均Bin%) >= 5.0%pt で警告

overrides:
  # 製品 nickname 単位で defaults を部分上書きできる
  Product-A:
    bin_surge:
      delta_pct: 3.0
